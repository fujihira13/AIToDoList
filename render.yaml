services:
  - type: web
    name: eisenhower-board
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_ENABLED
        value: "false"

